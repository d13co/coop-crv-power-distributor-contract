# Bootstrap SC and accounts

## Requirements

A `sandbox` script in your local `$PATH` that points to an algod sandbox.

See `sandbox.template` for a sample script that redirects to a local directory
